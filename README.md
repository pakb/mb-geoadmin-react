# geoadmin with Mapbox and React

This is a proof of concept of a map viewer similar to https://map.geo.admin.ch but written with React and with Mapbox as its map engine.

## how to run locally

Use `npm start`, the webpage should open automatically in your default browser